import { SupabaseRepositoryFactory } from '../../repositories/factories/SupabaseRepositoryFactory';

// Creamos una instancia de la fábrica de repositorios
export const repositoryFactory = new SupabaseRepositoryFactory();
